# monitoring/grafana/provisioning/datasources/prometheus.yml
# Auto-provisions Prometheus as the default datasource in Grafana

apiVersion: 1

datasources:
  - name:      Prometheus
    type:      prometheus
    access:    proxy
    url:       http://prometheus:9090
    isDefault: true
    editable:  false
    jsonData:
      httpMethod:       POST
      timeInterval:     "15s"
      queryTimeout:     "60s"
